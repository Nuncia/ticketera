import { useMemo, useContext } from 'react';
import { TicketContext } from '../context/ticketContext';

const UseRows = () => {
   // const { listadoTicket } = useContext(TicketContext);
   // const rows = useMemo(() => [...listadoTicket], [listadoTicket]);
   // return rows;
};

export default UseRows;
